# Core dependencies
flask>=3.0.0
pyyaml>=6.0
croniter>=2.0.0

# Scheduling (replaces system cron)
apscheduler>=3.10.0

# Configuration management & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# WSGI HTTP Server (replaces Flask dev server)
gunicorn>=21.0.0

# Rate limiting
flask-limiter>=3.0.0

# Authentication
flask-login>=0.6.0

# Type checking
mypy>=1.5.0

# Testing dependencies
pytest>=8.0.0
pytest-mock>=3.14.0
pytest-cov>=4.0.0

# Code quality
black>=23.0.0
ruff>=0.1.0
